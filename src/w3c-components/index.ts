import { AbstractButton } from './abstract-button';
import { AbstractButtonTimeout } from './abstract-button-timeout';

window.customElements.define(AbstractButton.tagName, AbstractButton);
window.customElements.define(AbstractButtonTimeout.tagName, AbstractButtonTimeout);
